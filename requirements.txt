# Compatible-release pins (~=) against the versions the pipeline is
# developed and tested with — yfinance/akshare especially have a history
# of breaking minor releases.
yfinance~=1.5.2
pandas~=3.0.5
requests~=2.34.2
cot-reports~=0.1.3
openpyxl~=3.1.5
akshare~=1.18.80
plotly~=6.9.0
jinja2~=3.1.6
beautifulsoup4~=4.15.0
xlrd~=2.0.2
pypdf~=6.14.2
pyyaml~=6.0.3
# Loads the local .env before config.py binds its key constants. Inert in CI,
# which sets its keys from GitHub secrets and ships no .env.
python-dotenv~=1.2.3
