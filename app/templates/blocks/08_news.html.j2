{# 08 News — reserved. Never `ok`: no layer in this stack ingests text, and
   choosing sources, licences, parsing and dedupe is a separate map. The slot
   exists so the layout is right the day it is filled. #}
