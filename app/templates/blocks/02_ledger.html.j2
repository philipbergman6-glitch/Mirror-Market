{# 02 Propagation ledger. No `ok` branch exists yet: M12 #161 has not chosen
   which counterpart legs sit on a given market's ledger, so every render goes
   through _block.html.j2's empty state. The partial is here so the file set
   matches the block set — a missing file would read as an oversight. #}
