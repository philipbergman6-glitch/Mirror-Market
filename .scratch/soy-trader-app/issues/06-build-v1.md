# 06 — Build v1

Type: task
Status: open
Blocked by: 05

## Question

Build the v1 local web app per `spec.md`: runnable with one command, opens on the command center, drill-down screens per the v1 scope, reading the live database. Resolved when the human has opened it, scanned real data, and accepted it as the replacement for the static dashboard.
