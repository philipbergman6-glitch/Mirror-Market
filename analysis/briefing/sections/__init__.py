"""Briefing section modules.

Each module exports a `format(...)` function (or a small number of named
formatters for closely-related sub-sections) that takes the data it
needs and returns a formatted text block. Sections are independently
testable — none depend on each other.
"""
